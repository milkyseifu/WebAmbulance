<form method="post" action="{{ path('ambulance_delete', {'id': ambulance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ambulance.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
