{% extends 'dashboard/index.html.twig' %}

{% block title %}Edit Hospital{% endblock %}

{% block body %}

    <div class="main-panel">
        <div class="content-wrapper">
            <div class="row">
                <div class="col-12 grid-margin stretch-card">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title">Edit Hospital Form</h4>
                            <p class="card-description">
                                Edit Hospital Form
                            </p>
                            {{ include('hospital/_form.html.twig', {'button_label': 'Update'}) }}

                            <a href="{{ path('hospital_index') }}" class="btn btn-link">back to list</a>

                            {{ include('hospital/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
